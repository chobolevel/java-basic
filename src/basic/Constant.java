package basic;

import java.util.UUID;

public class Constant {
    public static void main(String[] args) {
        
        final int MAX_NUM = 100;
        final int MIN_NUM;
        
        MIN_NUM = 0;

        System.out.println(MAX_NUM);
        System.out.println(MIN_NUM);
        
        // MAX_NUM = 1000; 한 번 지정한 상수에 대해서는 재정의가 불가능함
        
    }
}
